# Configuration Notes

Replace these placeholders after choosing the exact target:

- `CORE`: use the core name expected by your installed Nuclei SDK package
- `BOARD`: use the real evaluation board or SoC target
- `DOWNLOAD`: choose the load method that matches your runtime environment

Do not guess these values during pre-install preparation. Confirm them against the
specific SDK release and board support package you install.
